server:
  port: 15001

spring:
  cloud:
    config:
      server:
        git:
          uri: ${HOME}/enhanced-todo-app-config
          search-paths: "*server"
          default-label: rahat_multi_module